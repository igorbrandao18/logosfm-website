services:
  azurecast:
    image: azuracast/azuracast:latest
    container_name: azurecast-streaming
    restart: unless-stopped
    ports:
      - "8000:80"
    environment:
      - AZURACAST_DB_PASSWORD=changeme
      - AZURACAST_ADMIN_EMAIL=admin@brandaodeveloper.com.br
      - AZURACAST_ADMIN_PASSWORD=changeme
    volumes:
      - ./data:/var/azuracast/www
      - ./data/mysql:/var/lib/mysql
      - ./data/stations:/var/azuracast/stations
    networks:
      - azurecast-network

networks:
  azurecast-network:
    driver: bridge

